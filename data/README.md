This folder is used by `server.py` for local file CRUD operations.

- All files created by `local_file_crud` are stored here.
- Keep test files here (for example: `tata_sons_ownership.txt`).
- Path traversal is blocked; tool writes are sandboxed to this directory.
